# Orchestration: SaaS TCO Lab Final V2 Revenue Cells 14-day Sprint

## Execution Rules

- Keep the original objective intact.
- Ask for approval before risky, expensive, external, or destructive actions.
- Keep immediate blocking work local.
- Delegate only bounded, disjoint, materially useful packets.
- Integrate packet results before final verification.
- No subagent packets are used in this run; all overlapping dirty-tree edits remain under one integrator.
- No commit, push, deploy, indexing action, CTA activation, GA4 admin write, or external post without an exact Human token.

## Branching Rules

- Continue on the current branch because the existing dirty changes overlap the requested scope.
- Do not create a second worktree that would silently omit untracked dependencies.
- Do not prune the stale `/private/tmp/saastcolab-index-release.vCa7dS` worktree record.

## Packet Prompts

Not used. Multi-agent delegation was not requested for this instruction and would increase overlap risk.

## Completion Audit

- Compare final touched files to the baseline 46 tracked modifications and untracked set.
- Verify no staged changes and no destructive Git command.
- Run focused tests followed by `./scripts/check-all.sh`.
- Distinguish local-only artifacts from production facts in the final report.
